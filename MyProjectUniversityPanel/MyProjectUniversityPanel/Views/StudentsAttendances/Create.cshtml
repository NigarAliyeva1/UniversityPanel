@TempData["message"]
@*
@using (Html.BeginForm())
{

    <h2>Date :</h2>
    <input type="date" id="attendanceDate" name="attendanceDate" />

    <table class="table">


        <tr>
            <th>
                Student Name
            </th>
            <th>Mark Attendances</th>
        </tr>

        @foreach (var student in ViewBag.Students)
        {
            <tr>
                <td>
                    @student.FullName
                </td>
                <td>
                    <input type="checkbox" id="checkes" name="checkes" value="@student.Id" />
                </td>
            </tr>

        }

    </table>

    <input type="submit" value="Mark" id="btnMark" />

}*@



@model StudentsAttendance

<div class="row">
    <div class="col-md-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Create Department</h4>
                <p class="card-description"></p>
                <form class="forms-sample" method="post" enctype="multipart/form-data">
                    <table class="table" id="myTable">
                        <thead>
                            <tr>
                                <th>
                                    Student Name
                                </th>
                                <th>Mark Attendances</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var student in ViewBag.Students)
                            {
                                <tr>
                                    <td>
                                        @student.FullName
                                    </td>
                                    <td>
                                        <input type="checkbox" id="checkes" name="checkes" value="@student.Id" />
                                    </td>
                                </tr>

                            }
                        </tbody>
                    </table>
                    <button type="submit" class="btn btn-gradient-primary me-2">Submit</button>
                    <a asp-action="Index" class="btn btn-light">Go back</a>
                </form>
            </div>
        </div>
    </div>
</div>
@section Scripts{
    <partial name="_ValidationScriptsPartial" />
}
@*<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <div class="d-flex justify-content-between">
                    <h4 class="card-title">Mark Attendances</h4>
                    <input type="submit" value="Mark" id="btnMark" />
                </div>
                <div class="d-flex justify-content-center">
                    <h2>Date :</h2>
                    <input type="date" id="attendanceDate" name="attendanceDate" />
                </div>


                <table class="table" id="myTable">
                    <thead>
                        <tr>
                            <th>
                                Student Name
                            </th>
                            <th>Mark Attendances</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var student in ViewBag.Students)
                        {
                            <tr>
                                <td>
                                    @student.FullName
                                </td>
                                <td>
                                    <input type="checkbox" id="checkes" name="checkes" value="@student.Id" />
                                </td>
                            </tr>

                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
*@
@*<script src="~/Scripts/jquery-1.10.2.min.js"></script>*@
@*<script>
    $(document).ready(function (event) {

        $("#btnMark").prop('disabled', true);
    });
    $("#attendanceDate").change(function (event) {
        $("#btnMark").removeProp('disabled');
    });
</script>

*@
<script type="text/javascript">
    $(document).ready(function () {
        $('#myTable').DataTable({
            dom: 'Bfrtip',
            buttons: [
                'copy', 'csv', 'excel', 'pdf', 'print'
            ]
        });
    });

</script>